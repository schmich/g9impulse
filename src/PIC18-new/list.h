#ifndef LIST_H
#define LIST_H

#include "common.h"

/*typedef struct Node
{
    void* data;
    Node* next;
} Node;

typedef struct List
{
    uint16 size;
    uint16 capacity;
} List;

List createList();
void appendElement(*/

#endif // LIST_H
